name: Gosec

on: push
    runs-on: ubuntu-latest 
    steps:
    - uses: actions/checkout@master
    - name: check
      uses: grandcolline/golang-github-actions@v1.0.0
      with:
        run: sec
        flags: "-exclude=G104"
